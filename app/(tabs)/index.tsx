import { images } from "@/constants/images";
import { Link } from "expo-router";
import { ScrollView, Text, View, Image, StyleSheet} from "react-native";

export default function Index() {
  return (
    
      <View style={{backgroundColor: 'rgb(0, 9, 29)', flex:1}}>
        
      </View>
    
  );
}
